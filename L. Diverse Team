#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, k;
    cin >> n >> k;

    int a[105];
    int indexList[105];
    bool taken[101] = {0};

    for (int i = 1; i <= n; i++) {
        cin >> a[i];
    }

    int count = 0;
    for (int i = 1; i <= n && count < k; i++) {
        if (!taken[a[i]]) {
            taken[a[i]] = true;
            indexList[count] = i;
            count++;
        }
    }

    if (count < k) {
        cout << "NO\n";
    } else {
        cout << "YES\n";
        for (int i = 0; i < k; i++) {
            cout << indexList[i] << (i+1==k ? '\n' : ' ');
        }
    }

    return 0;
}


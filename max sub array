#include <bits/stdc++.h>
using namespace std;

int main() {
    int T;
    cin >> T;
    while (T--) {
        int N;
        cin >> N;
        int A[105];  
        for (int i = 0; i < N; i++) cin >> A[i];

        int total = N * (N + 1) / 2; 
        int count = 0;              

        for (int i = 0; i < N; i++) {
            int currentMax = A[i];
            for (int j = i; j < N; j++) {
                currentMax = max(currentMax, A[j]);
                cout << currentMax;
                count++;
                if (count < total) cout << " "; 
            }
        }
        cout << "\n";
    }
    return 0;
}
